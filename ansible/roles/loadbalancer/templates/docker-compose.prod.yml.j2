# OVERRIDE: producción.
services:
  haproxy:
    restart: unless-stopped
    network_mode: host
    user: root
    cap_add:
      - NET_BIND_SERVICE
    volumes:
      - {{ srv_base }}/loadbalancer/haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
