[defaults]
inventory = inventories/produccion/hosts.ini
roles_path = roles
remote_user = devops
private_key_file = ~/.ssh/id_ed25519
host_key_checking = False
interpreter_python = auto_silent

[privilege_escalation]
become = True
become_method = sudo
